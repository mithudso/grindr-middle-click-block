# Codebase overview

Every file in the repository and what it is for.

Generated by `scripts/gen-codebase-index.mjs`.
`high_signal_file_index.json` is the machine-readable twin.

## `(root)`

| File | Purpose |
|---|---|
| `.editorconfig` | Cross-editor formatting. |
| `.gitattributes` | Line endings and binary markers. |
| `.gitignore` | Ignore rules, including local captures and the identifier mapping. |
| `.nvmrc` | Pinned Node major for CI. |
| `AGENTS.md` | No repo-local agents. Points at the script's own self-diagnosis surfaces, which are what to reach for instead. |
| `CLAUDE.md` | Working rules for agents and contributors: read the interaction library first, and the five rules that came from real failures. |
| `CODE_OF_CONDUCT.md` | Conduct, plus the non-negotiable rule about not posting other people's identifiers. |
| `CONTRIBUTING.md` | How to change a matcher without repeating this project's history: read the interaction library first, never match by substring, refuse rather than guess. |
| `GEMINI.md` | The same working rules, for Gemini CLI. |
| `Grindr Middle-Click Block.user.js` | The entire userscript. One IIFE: observation (patched fetch/XHR/WebSocket), actuation (block queue, greet flow, album rotation, DOM sweep), and surface (hotkeys, HUD, diagnostic recorder). |
| `LICENSE` | MIT. |
| `README.md` | Project overview, feature table, HUD, console API, and what the tests deliberately do not cover. |
| `package.json` | Scripts only — check / test / verify. No dependencies. |

## `.github`

| File | Purpose |
|---|---|
| `CODEOWNERS` | Reviewer mapping. |
| `PULL_REQUEST_TEMPLATE.md` | PR template. Asks for evidence, because "it looks right" has been wrong here. |
| `SECURITY.md` | How to report, and what is actually worth reporting. |
| `copilot-instructions.md` | The same working rules, for Copilot CLI. |
| `dependabot.yml` | Actions only — the script has no runtime dependencies. |

## `.github/ISSUE_TEMPLATE`

| File | Purpose |
|---|---|
| `bug_report.md` | Bug template — asks for a diagnostic capture, redacted. |
| `feature_request.md` | Feature template — asks how it should fail safe. |

## `.github/workflows`

| File | Purpose |
|---|---|
| `ci.yml` | Syntax check, test suite, docs drift check. |

## `dist`

| File | Purpose |
|---|---|
| `grindr.esm.js` | AUTO-GENERATED by scripts/build-lib.mjs — do not edit. Source: lib/*.js |
| `grindr.global.js` | AUTO-GENERATED by scripts/build-lib.mjs — do not edit. Source: lib/*.js |

## `docs`

| File | Purpose |
|---|---|
| `ARCHITECTURE.md` | Layers, the identity problem and how it is resolved, the two block tiers, the enforcement sweep, and the decisions worth knowing. |
| `DEVELOPMENT.md` | The edit-verify-paste-record loop, repo layout, conventions, and debugging aids. |
| `INSTALLATION.md` | Install, verify, first-run setup, upgrade, and a real uninstall including stored state. |
| `SECURITY.md` | Threat model. The interesting risks are acting on the wrong profile and the page abusing our own functions. |
| `TESTING.md` | What the suite covers, why coverage is not a line percentage, and what is left to live verification. |
| `codebase-overview.md` | This file map. |
| `external-calls.md` | Every request the script makes, its failure handling, the rate limiter, auth handling, and the localStorage keys. |
| `function-reference.md` | Every named function, generated from source. |
| `grindr-dom-and-api.md` | The interaction library: observed routes, DOM shapes, endpoints and traps, each with how it was confirmed. |
| `high_signal_file_index.json` | Machine-readable index of the same, for retrieval. |
| `logging.md` | Levels, why every line reaches the recorder before the verbosity gate, the partitioned buffer, and what is never logged. |

## `docs/superpowers/plans`

| File | Purpose |
|---|---|
| `2026-08-30-grindr-interaction-library.md` | (auto) Document. |

## `docs/superpowers/specs`

| File | Purpose |
|---|---|
| `2026-08-30-grindr-interaction-library-design.md` | (auto) Document. |

## `lib`

| File | Purpose |
|---|---|
| `albums.js` | Private-album share client. A 403 on an album op means the album is not yours (deleted/invalid) — the caller should retire it rather than retry. |
| `auth.js` | (auto) Exports createAuth. |
| `blocks.js` | (auto) Exports createBlocks. |
| `chat.js` | Chat HTTP surface + conversation-id helpers. NOTE: sending a message produces no HTTP request (it goes over the WebSocket) — see compose.greet. The only HTTP trace of activity is the typing indicator. |
| `compose.js` | Composer + send-button resolution. Two composers can be on screen at once (the open profile's wide input and the floating chat drawer's narrow textarea); this prefers the profile composer so a greeting is not sent into the wrong drawer c… |
| `dom.js` | Pure DOM helpers: profile-id and cascade-tile resolution, plus route detection. The tile resolution is bounded to avoid the "sidebar trap" — walking up from a conversation avatar to a huge ancestor (the inbox UL) and hiding the chat list. |
| `errors.js` | Typed errors for the Grindr interaction library. Never place a token in a message — callers surface these to logs/UIs. |
| `index.js` | (auto) Exports VERSION, createClient. |
| `limiter.js` | Opt-in rate limiter. Grindr force-logs-out on bursts of writes, so callers that issue many block/hide/album calls should route them through here: it serializes calls behind a minimum interval and a rolling hourly cap. |
| `observe.js` | Opt-in traffic observer: patches fetch / XHR / WebSocket.send to auto-capture the Grindr3 auth headers, tap hides/blocks list responses, and observe outbound WS frames. Every patch is guarded for frozen intrinsics (SES/lockdown). Uses a … |
| `profiles.js` | Profile / cascade / views client. @param {ReturnType<import('./auth.js').createAuth>} auth |
| `reconcile.js` | (auto) Exports idsFromListPayload, reconcileTiers. |

## `notes`

| File | Purpose |
|---|---|
| `archive 2.md` | (auto) Document. |
| `archive.md` | (auto) Document. |
| `now 2.md` | (auto) Document. |
| `now.md` | (auto) Document. |
| `recent 2.md` | (auto) Document. |
| `recent.md` | (auto) Document. |
| `today-2026-06-14.done.md` | (auto) Document. |
| `today-2026-06-15 2.done.md` | (auto) Document. |
| `today-2026-06-15.done.md` | (auto) Document. |
| `today-2026-06-20.done.md` | (auto) Document. |
| `today-2026-06-21.done.md` | (auto) Document. |
| `today-2026-06-22.done.md` | (auto) Document. |
| `today-2026-06-23.done.md` | (auto) Document. |
| `today-2026-07-20.done.md` | (auto) Document. |
| `today-2026-07-24.done.md` | (auto) Document. |
| `today-2026-08-01.done.md` | (auto) Document. |
| `today-2026-08-23.md` | (auto) Document. |

## `scripts`

| File | Purpose |
|---|---|
| `build-lib.mjs` | Zero-dependency build: concatenate the lib/ ES modules (all first-party, no external imports) in dependency order into a single-file ESM bundle and a window.Grindr IIFE global bundle. No bundler. |
| `check-docs.mjs` | CI drift gate: functions documented, version consistent, reference current, links resolve, no real identifiers. |
| `gen-codebase-index.mjs` | Regenerates this overview and the file index. |
| `gen-function-reference.mjs` | Regenerates the function reference from source. |

## `test`

| File | Purpose |
|---|---|
| `helpers.test.cjs` | The pure helpers, imported for real. Each block states why the helper exists and what it must guarantee. |
| `keymap.test.cjs` | Hotkey routing, alias matching, rebinding, and pass-through when nothing can be acted on. |
| `load.test.cjs` | Boots the whole IIFE — catches "the script dies at document-start", which a syntax check cannot. |
| `queue-order.test.cjs` | Block-queue ordering and the split hourly budget. WHY THIS FILE EXISTS: a capture of "blocks are not working at all" showed they were never failing. One shared rolling-hour budget let the auto-drain — a background migration of ~1400 entr… |
| `regression.test.cjs` | Bugs that actually shipped, each named in a comment. |
| `settings.test.cjs` | Persisted settings, greetings, block-tier invariants, auto-drain, and the recorder. |
| `stubs.cjs` | Minimal browser stubs. Deliberately dumb: every DOM query returns empty, so tests exercise logic rather than a simulated page. |

## `test/lib`

| File | Purpose |
|---|---|
| `albums.test.cjs` | (auto) Tests for `albums` — 4 cases. |
| `auth.test.cjs` | (auto) Tests for `auth` — 5 cases. |
| `blocks.test.cjs` | (auto) Tests for `blocks` — 5 cases. |
| `build.test.cjs` | (auto) Tests for `build` — 2 cases. |
| `chat.test.cjs` | (auto) Tests for `chat` — 4 cases. |
| `compose.test.cjs` | (auto) Tests for `compose` — 3 cases. |
| `dom.test.cjs` | (auto) Tests for `dom` — 5 cases. |
| `errors.test.cjs` | (auto) Tests for `errors` — 3 cases. |
| `index.test.cjs` | (auto) Tests for `index` — 3 cases. |
| `limiter.test.cjs` | (auto) Tests for `limiter` — 2 cases. |
| `observe.test.cjs` | (auto) Tests for `observe` — 2 cases. |
| `profiles.test.cjs` | (auto) Tests for `profiles` — 3 cases. |
| `reconcile.test.cjs` | (auto) Tests for `reconcile` — 2 cases. |
| `stubs.cjs` | (auto) No description recorded. |

# Codebase overview

Every file in the repository and what it is for.

Generated by `scripts/gen-codebase-index.mjs`.
`high_signal_file_index.json` is the machine-readable twin.

## `(root)`

| File | Purpose |
|---|---|
| `.editorconfig` | Cross-editor formatting. |
| `.gitattributes` | Line endings and binary markers. |
| `.gitignore` | Ignore rules, including local captures and the identifier mapping. |
| `.nvmrc` | Pinned Node major for CI. |
| `AGENTS.md` | No repo-local agents. Points at the script's own self-diagnosis surfaces, which are what to reach for instead. |
| `CLAUDE.md` | Working rules for agents and contributors: read the interaction library first, and the five rules that came from real failures. |
| `CODE_OF_CONDUCT.md` | Conduct, plus the non-negotiable rule about not posting other people's identifiers. |
| `CONTRIBUTING.md` | How to change a matcher without repeating this project's history: read the interaction library first, never match by substring, refuse rather than guess. |
| `GEMINI.md` | The same working rules, for Gemini CLI. |
| `Grindr Middle-Click Block.user.js` | The entire userscript. One IIFE: observation (patched fetch/XHR/WebSocket), actuation (block queue, greet flow, album rotation, DOM sweep), and surface (hotkeys, HUD, diagnostic recorder). |
| `LICENSE` | MIT. |
| `README.md` | Project overview, feature table, HUD, console API, and what the tests deliberately do not cover. |
| `package.json` | Scripts only — check / test / verify. No dependencies. |

## `.github`

| File | Purpose |
|---|---|
| `CODEOWNERS` | Reviewer mapping. |
| `PULL_REQUEST_TEMPLATE.md` | PR template. Asks for evidence, because "it looks right" has been wrong here. |
| `SECURITY.md` | How to report, and what is actually worth reporting. |
| `copilot-instructions.md` | The same working rules, for Copilot CLI. |
| `dependabot.yml` | Actions only — the script has no runtime dependencies. |

## `.github/ISSUE_TEMPLATE`

| File | Purpose |
|---|---|
| `bug_report.md` | Bug template — asks for a diagnostic capture, redacted. |
| `feature_request.md` | Feature template — asks how it should fail safe. |

## `.github/workflows`

| File | Purpose |
|---|---|
| `ci.yml` | Syntax check, test suite, docs drift check. |

## `docs`

| File | Purpose |
|---|---|
| `ARCHITECTURE.md` | Layers, the identity problem and how it is resolved, the two block tiers, the enforcement sweep, and the decisions worth knowing. |
| `DEVELOPMENT.md` | The edit-verify-paste-record loop, repo layout, conventions, and debugging aids. |
| `INSTALLATION.md` | Install, verify, first-run setup, upgrade, and a real uninstall including stored state. |
| `SECURITY.md` | Threat model. The interesting risks are acting on the wrong profile and the page abusing our own functions. |
| `TESTING.md` | What the suite covers, why coverage is not a line percentage, and what is left to live verification. |
| `codebase-overview.md` | This file map. |
| `external-calls.md` | Every request the script makes, its failure handling, the rate limiter, auth handling, and the localStorage keys. |
| `function-reference.md` | Every named function, generated from source. |
| `grindr-dom-and-api.md` | The interaction library: observed routes, DOM shapes, endpoints and traps, each with how it was confirmed. |
| `high_signal_file_index.json` | Machine-readable index of the same, for retrieval. |
| `logging.md` | Levels, why every line reaches the recorder before the verbosity gate, the partitioned buffer, and what is never logged. |

## `scripts`

| File | Purpose |
|---|---|
| `check-docs.mjs` | CI drift gate: functions documented, version consistent, reference current, links resolve, no real identifiers. |
| `gen-codebase-index.mjs` | Regenerates this overview and the file index. |
| `gen-function-reference.mjs` | Regenerates the function reference from source. |

## `test`

| File | Purpose |
|---|---|
| `helpers.test.cjs` | The pure helpers, imported for real. Each block states why the helper exists and what it must guarantee. |
| `keymap.test.cjs` | Hotkey routing, alias matching, rebinding, and pass-through when nothing can be acted on. |
| `load.test.cjs` | Boots the whole IIFE — catches "the script dies at document-start", which a syntax check cannot. |
| `regression.test.cjs` | Bugs that actually shipped, each named in a comment. |
| `settings.test.cjs` | Persisted settings, greetings, block-tier invariants, auto-drain, and the recorder. |
| `stubs.cjs` | Minimal browser stubs. Deliberately dumb: every DOM query returns empty, so tests exercise logic rather than a simulated page. |
